import React from 'react';

const messageChat = ({ name, message }) => (
    
    <p style={{ color: 'rgb(214 6 76)' }}>
        <strong>{name}</strong>&nbsp;<em>{message}</em>
    </p>

)



export default messageChat;